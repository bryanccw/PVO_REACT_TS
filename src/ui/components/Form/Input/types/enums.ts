enum InputValidations {
  VALID = 'valid',
  INVALID = 'invalid',
  DEFAULT = 'default',
}
enum InputTypes {
  TEXT = 'text',
  NUMBER = 'number',
  EMAIL = 'email',
}
enum InputAutoComplete {
  ON = 'on',
  OFF = 'off',
}

export { InputValidations, InputTypes, InputAutoComplete };
